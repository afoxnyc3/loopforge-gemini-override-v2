# gemini-override-v2

Build a minimal CLI tool in Node.js that converts Markdown files to HTML. Support headings, bold, italic, code blocks, and links. Include a --watch flag for auto-recompile on file changes.

Built by [LoopForge](https://github.com/partnerandagent/loopforge) — autonomous multi-agent development.
